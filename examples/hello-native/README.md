# hello-native

Runs a tier-1 `native.echo` node on a local controller machine, then a shell node that runs only when the native node reports `Changed`.

```bash
cargo run -p hello-native -- apply
```
